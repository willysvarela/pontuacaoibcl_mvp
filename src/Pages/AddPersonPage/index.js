import React from 'react';
import {View} from 'react-native';
import {Container, Content, Header, Form} from 'native-base';
import getRealm from '../config/realm';

const AddPersonPage = props => {
  return (
    <Container>
      <Header></Header>
      <Content></Content>
      <Form>

      </Form>
    </Container>
  );
};
